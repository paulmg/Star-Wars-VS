query allEntries($first: Int!) {
  allFilms(filter: {imageURL_not: ""}, orderBy: wins_DESC, first: $first) {
    id
    director
    episodeId
    imageURL
    losses
    producers
    releaseDate
    title
    wins
  }
}
